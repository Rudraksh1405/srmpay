import { useEffect, useState } from 'react'
import { mockApi } from '../../mockData'
export default function MerchantRequests() { const [requests, setRequests] = useState([]); useEffect(() => { mockApi.getMerchantRequests().then(setRequests) }, []); const resolve = (id) => setRequests((items) => items.filter((item) => item._id !== id)); return <section className="glass-card content-card"><p className="eyebrow">Merchant requests</p><h2 className="card-title">Awaiting review</h2>{requests.map((request) => <div className="row" key={request._id}><span><b>{request.businessName}</b><br /><small>{request.category} · {request.location}</small></span><span><button className="secondary-btn" onClick={() => resolve(request._id)}>Reject</button> <button className="primary-btn" onClick={() => resolve(request._id)}>Approve</button></span></div>)}</section> }
