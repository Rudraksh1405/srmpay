import { Building2, ClipboardCheck, LayoutDashboard, LogOut, Store } from 'lucide-react'
import { NavLink, Outlet } from 'react-router-dom'
import { useAuth } from '../../contexts/AuthContext'
import { useTheme } from '../../contexts/ThemeContext'
export default function AdminDashboard() { const { logout } = useAuth(); const { toggle } = useTheme(); const links = [['', LayoutDashboard, 'Dashboard'], ['merchants', Store, 'View Merchants'], ['requests', ClipboardCheck, 'Merchant Requests'], ['merchants', Building2, 'Merchant Details']]; return <div className="layout admin-layout"><aside className="sidebar"><p className="portal-name">SRMPAY ADMIN</p>{links.map(([to, Icon, label], index) => <NavLink end={index === 0} className="side-link" to={to} key={label}><Icon size={19} /><span>{label}</span></NavLink>)}<button className="side-logout" onClick={logout}><LogOut size={18} /> <span>Logout</span></button></aside><main className="portal-content"><div className="portal-topbar"><div><p className="eyebrow">Administrator portal</p><h1 className="page-title">Platform overview</h1></div><button className="icon-button" onClick={toggle}>◐</button></div><Outlet /></main></div> }
