import { useState } from 'react'
import { useAuth } from '../../contexts/AuthContext'
export default function AdminLogin() { const [username, setUsername] = useState('admin'); const [password, setPassword] = useState('admin123'); const [error, setError] = useState(''); const { login } = useAuth(); return <main className="login-page"><form className="glass-card login-card" onSubmit={(event) => { event.preventDefault(); if (username === 'admin' && password === 'admin123') login({ role: 'admin' }); else setError('Use the demo credentials shown below.') }}><p className="eyebrow">Administrator access</p><h1 className="page-title">Sign in.</h1><p className="demo-note">Demo credentials: admin / admin123</p><label className="field">Username<input value={username} onChange={(event) => setUsername(event.target.value)} /></label><label className="field">Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} /></label>{error && <p>{error}</p>}<button className="primary-btn">Sign in</button></form></main> }
