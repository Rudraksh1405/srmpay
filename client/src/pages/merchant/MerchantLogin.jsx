import { useState } from 'react'
import { useAuth } from '../../contexts/AuthContext'
export default function MerchantLogin() { const [username, setUsername] = useState('merchant'); const [password, setPassword] = useState('password'); const { login } = useAuth(); return <main className="login-page"><form className="glass-card login-card" onSubmit={(event) => { event.preventDefault(); login({ role: 'merchant' }) }}><p className="eyebrow">Merchant access</p><h1 className="page-title">Welcome back.</h1><p className="demo-note">Demo credentials: merchant / password</p><label className="field">Username<input value={username} onChange={(event) => setUsername(event.target.value)} /></label><label className="field">Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} /></label><button className="primary-btn">Sign in</button></form></main> }
