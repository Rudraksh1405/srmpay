import { useState } from 'react'
import { useAuth } from '../../contexts/AuthContext'
export default function StudentLogin() { const [register, setRegister] = useState(false); const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const { login } = useAuth(); const submit = async (event) => { event.preventDefault(); if (!/@srmist\.edu\.in$/.test(form.email)) return setError('Use your @srmist.edu.in email address.'); const response = await fetch(`/api/v1/student/${register ? 'register' : 'login'}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) }); const data = await response.json(); if (!response.ok) return setError(data.message); login({ role: 'student', email: data.student.email, token: data.token }) }; return <main className="login-page"><form className="glass-card login-card" onSubmit={submit}><p className="eyebrow">Student portal</p><h1 className="page-title">{register ? 'Create account.' : 'Welcome back.'}</h1>{register && <label className="field">Name<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></label>}<label className="field">SRM email<input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /></label><label className="field">Password<input required type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} /></label>{error && <p className="demo-note">{error}</p>}<button className="primary-btn">{register ? 'Register' : 'Login'}</button><button type="button" className="secondary-btn" onClick={() => { setRegister(!register); setError('') }}>{register ? 'I already have an account' : 'Register instead'}</button></form></main> }
